# Feature: add_planned_workout_id_and_notes_to_activity

## Objective
Add `planned_workout_id` (nullable FK to PlannedWorkout) and `notes` (nullable Text) fields to the Activity model and schemas to support Phase 1f activity logging and post-workout analysis. The FK constraint is deferred (nullable, no enforcement) to allow migration before PlannedWorkout exists.

---

## Models
1. Add planned_workout_id and notes fields to Activity model
   - Objective: Add nullable planned_workout_id UUID FK and notes Text columns to Activity model
   - File: `app/models/activity.py` [MODIFY]
   - Actions:
     - Add `planned_workout_id` as Mapped[Optional[uuid.UUID]] with UUID column, ForeignKey("planned_workouts.id", ondelete="SET NULL"), nullable=True
     - Add `notes` as Mapped[Optional[str]] with Text column, nullable=True

---

## Schemas
2. Add planned_workout_id and notes to all Activity schemas
   - Objective: Extend ActivityBase, ActivityCreate, ActivityUpdate, and ActivityResponse with new optional fields
   - File: `app/schemas/activity.py` [MODIFY]
   - Actions:
     - Add `planned_workout_id: Optional[uuid.UUID] = None` to ActivityBase, ActivityCreate, ActivityUpdate, and ActivityResponse
     - Add `notes: Optional[str] = None` to ActivityBase, ActivityCreate, ActivityUpdate, and ActivityResponse

---

## Migration
3. Create migration for new Activity fields
   - Objective: Add planned_workout_id and notes columns to activities table
   - File: `migrations/versions/<autogenerated>_add_planned_workout_id_and_notes_to_activities.py` [CREATE]
   - Actions:
     - Add nullable `planned_workout_id` column of type UUID
     - Add nullable `notes` column of type Text
     - Downgrade: drop `notes` and `planned_workout_id` columns from activities table
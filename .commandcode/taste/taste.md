- Prefers a multi-agent architecture with structured sub-agent delegation and clear separation of concerns (specialized sub-agents for different domains like state exploration, documentation, impact analysis, contract verification, code structure). Confidence: 0.9
- Relies on MCP-based codebase/documentation indexing servers for context retrieval (has a "big MCP server" with code and documentation indexed). Confidence: 0.9
- Prefers preserving existing workflow patterns and processes when migrating between tools (wants to "maintain a similar process" rather than redesign workflows). Confidence: 0.8
- Uses model pinning per agent (different models assigned to different agents based on task complexity — cheap/fast models for lookup sub-agents, session model for the primary architect agent). Confidence: 0.8
- Uses a skills/instructions system that agents load on demand for specific tasks (implementation plan templates, resolution procedures, handoff blocks, architecture decision templates). Confidence: 0.8
- Prefers consolidated, synthesized documentation over scattered or verbatim copies. When migrating or updating knowledge files, expects the assistant to extract and condense relevant information into a single authoritative file rather than maintaining separate scattered configs. Confidence: 0.8
- Prefers restricting agents from using native file-reading tools (read_file, glob, grep) in favor of MCP tools for codebase exploration. Wants agent tool allowlists to exclude raw file access so that context retrieval goes through the indexed MCP server instead. Motivation: MCP tools support batching and bulk extraction, reducing roundtrips and improving token efficiency. Confidence: 0.9
- Prefers keeping broad tool access available as fallback even when delegation to sub-agents is the primary intended path. Does not want tools removed from an agent's allowlist to enforce architecture — prefers prompts to guide behavior, with tools remaining as safety nets. Confidence: 0.75
- Prefers agent tool allowlists to be surgically calibrated against the agent's prompt — only add MCP tools that are explicitly referenced in the prompt instructions, don't add unused tools for completeness, and don't miss any that are called. Cross-referencing the prompt text against the tool list is the expected workflow. Confidence: 0.85
- When migrating agents between frameworks, expects the assistant to trace the full dependency graph — sub-agents, skills, and any referenced tools — and migrate everything needed, not just the top-level agents explicitly requested. Confidence: 0.85
- After migration, expects a final audit pass across all migrated and pre-existing artifacts to catch stale platform-specific references (e.g. old `.opencode/` paths) and redundant content that duplicates a canonical source like AGENTS.md. Skills should reference the canonical source rather than re-state it. Confidence: 0.85
- Uses a three-tier model assignment pattern for agents: premium models (e.g. mimo-v2.5-pro) for complex reasoning/authoring agents, mid-tier models for mechanical work (e.g. diagnostics fixing), and flash/cheap models for pure read-only resolvers and lookups. Confidence: 0.8
- Values observability and traceability of agent internals — wants visibility into agent reasoning, subagent calls, and the detailed execution steps within subagents, not just final outputs. Expects permanent logs of subagent internal activity to be available for post-hoc inspection. Confidence: 0.85
- Identified a key platform constraint: Command Code unconditionally strips the `agent`/`agent_output` tools from subagents at runtime, even if listed in the agent's frontmatter `tools:` field. This makes multi-level delegation (subagent → sub-subagent) structurally impossible. Listing `"agent"` in an agent's tool allowlist is effectively dead config — it's always removed regardless. Confidence: 0.9
- As a consequence of the above: agent prompts must be audited against what tools the platform actually grants at runtime, not just the frontmatter `tools:` list. Dead protocol text — delegation instructions, references to subagent calls that the agent can't perform — wastes tokens and misleads model behavior. Prompts should be validated against the runtime tool set after platform filtering. Confidence: 0.85
- Measured the base system prompt (before any user context) at 41.5k tokens — a concrete token budget baseline. The platform's unconditional injection of all 68 MCP tool schemas (~8-12k+ tokens per turn) is identified as the primary source of this bloat. This is a distinct concern from AGENTS.md content duplication; it's about the platform's tool registry injection being task-irrelevant and unavoidable. Confidence: 0.9
- Wants post-execution verification and auditability — expects the ability to analyze session transcripts to confirm agent execution steps, verify all files created/amended, and cross-check claims against what actually happened on disk. Confidence: 0.85
- Uses model-assignment diversity across agents as a diagnostic signal — when sub-agents are pinned to different models than the primary agent, the absence of API calls to those models is concrete evidence that delegation didn't occur. Confidence: 0.8
- Validates agent execution correctness by inspecting output quality (wrong imports, unfixed diagnostics, hallucinated code) as a proxy for whether the agent followed its prescribed workflow and delegated properly. Confidence: 0.8
- Expects the same agent config (prompt + tool list + model assignment) to produce the same behavioral results across different platforms (e.g. OpenCode vs Command Code). When behavior diverges despite identical configuration, sees it as a platform bug or missing feature, not a config problem. Confidence: 0.75
- Prefers converting orchestrator agents (agents that coordinate multiple sub-agents) into skills loaded by the main agent, while keeping executor/resolver agents (leaf-node sub-agents) as actual subagents. This avoids platform-enforced single-level-deep delegation limits and eliminates wasted tokens on dead protocol text that can't execute. Confidence: 0.65
- Strongly avoids duplicating shared skills (like code-exploration) into individual agent prompts as a workaround for platform delegation limits. Shared skills must stay modular and reusable — inlining 300-400+ lines of shared skill content into each orchestrator agent is unacceptable prompt bloat. Platform limitations should not force architectural degradation. Confidence: 0.85
- Considers model diversity by cognitive role (architect vs coder) a hard architectural constraint, not an optimization. Architect models and coder models are fundamentally different — each role requires a model suited to its cognitive demands, and they cannot share a single session model without breaking the architecture. Converting role-specialized agents into skills that all run on the same session model is architecturally unacceptable because it collapses essential model diversity across roles. Confidence: 0.9
- Cost-efficiency is a primary driver for model-tier allocation — not just capability suitability. Using an expensive model (e.g. glm-5.2) to do large numbers of exploratory MCP tool calls that a cheap flash model (e.g. deepseek-v4-flash) could achieve at a fraction of the cost is architecturally unacceptable. The cost differential between model tiers is a binding constraint for how work is distributed across agents. Confidence: 0.9
- Prefers a session-per-role workflow pattern: dedicate a session to a specific cognitive role (test-writing, coding, architecture) with the appropriate model pinned, and use separate sessions for different roles. This avoids fighting platform delegation limits and avoids architectural degradation from trying to orchestrate multiple cognitive roles in a single session. Confidence: 0.7
- Considers AGENTS.md and taste.md to be sufficient persistent context across sessions — project knowledge, conventions, and architectural rules carried in these files as system prompt material make session-per-role viable without losing essential context. Session transcripts (exploration noise, intermediate decisions) are expendable; the canonical files hold what matters. Confidence: 0.6
- Concerned about token bloat when loading large skill prompts into the main agent's context — wants to understand the token/KB impact before committing to a skill-based approach. Adding hundreds of lines (~27 KB for a skill) to the base prompt is a significant cost consideration that must be evaluated upfront. Confidence: 0.8
- Prefers the platform to scope MCP tool schema injection to only tools relevant to the current task or loaded skill, rather than injecting all 68 tool descriptions and JSON schemas (~8-12k+ tokens) unconditionally every turn. AGENTS.md is explicitly NOT the problem — the user corrected this misattribution. The platform's tool registry injection wastes tokens by loading irrelevant tool schemas for every task regardless of need, and this is outside the user's control. Confidence: 0.9
- Prefers hard, platform-enforced tool restrictions (agent frontmatter `tools:` allowlists) over advisory skill instructions. Skills provide only instructional guidance that the AI can choose to follow; agent tool allowlists guarantee constraints at the platform level. Losing enforceable tool control when converting agents to skills is a meaningful architectural trade-off. Confidence: 0.85

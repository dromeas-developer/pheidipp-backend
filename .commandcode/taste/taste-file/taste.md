# Taste File
- Always reference the official OpenCode documentation at https://opencode.ai/docs/ and its subpages (agents, tools, skills, etc.) when working on OpenCode-related tasks. Confidence: 0.90
- Search for community/industry best practices around skills, subagents, and agents when designing OpenCode architectures — don't rely solely on official docs. Confidence: 0.85
- Role is OpenCode prompt/skill/subagent architect — the user builds and maintains sophisticated multi-agent OpenCode setups with delegation graphs, skill extraction, permission patterns, and model routing. Confidence: 0.80
- In OpenCode agent architectures, agents whose filenames start with `p-` are primary agents, never subagents. The naming prefix encodes the agent's role in the delegation graph. Confidence: 0.98
- Avoid duplication between agent prompts and skills: each concept lives in exactly one place. Agent prompts own process flow (which step does what, in what order). Skills own templates, content rules, and definitions. When both need the same information, the agent prompt delegates to the skill with a concise pointer rather than repeating the content. Confidence: 0.90
- Subagent prompts should minimize MCP/tool calls by using decision-tree patterns (one primary tool drives conditional follow-ups) rather than sequential checklists that encourage running every available tool. Confidence: 0.90
- Use the `steps:` field in subagent frontmatter to cap model turns and prevent runaway tool-calling loops. Confidence: 0.70
- For subagents that legitimately require many MCP calls to fulfill their purpose (e.g., impact analysis), avoid restrictive `steps:` limits — they block the agent from completing its work. Use `steps:` as a runaway-loop guard, not a tight budget. Confidence: 0.80

- Before modifying a subagent's behavior or tool-calling pattern, always first analyze the call sites — grep for every context where the subagent is invoked and confirm what callers actually expect. Base optimization decisions on what callers need from the output, not on what the subagent's current prompt instructs it to run. Confidence: 0.88
- When reducing subagent tool calls, completeness of the output takes priority over minimizing calls — never skip a section that a caller genuinely needs. Optimize for the minimum set of calls that still produces a complete report, not the absolute minimum calls possible. Confidence: 0.85
- When fixing a subagent's tool-calling behavior, fix both sides of the delegation contract: the subagent's prompt (what it does) AND the calling agents' prompts (what they ask for). Don't optimize one side in isolation — mismatched expectations between caller and subagent are the root cause of both over-calling and under-reporting. Confidence: 0.80
- When debugging subagent behavior or building a case for optimization, always verify with actual return values from session traces — never infer what a tool "should" have returned based on its name or purpose. A claim like "get_change_impact returned the data" must be backed by showing the actual response. Guessing leads to wrong root-cause analysis. Confidence: 0.92
- When proposing changes, explain what you propose and why before executing — don't jump straight to editing. The user wants to understand the reasoning and validate the approach before any code or config is modified. This applies especially to agent prompt changes where the reasoning reveals whether the diagnosis is correct. Confidence: 0.88

- When an agent family has multiple modes that share substantial instruction blocks (meta-instructions, standards, contracts), extract a shared-core skill following the `{family}-shared-core` naming pattern. Each mode keeps only mode-specific dispatch logic; the shared core is loaded on demand. This avoids maintaining the same content across multiple agent prompts. Confidence: 0.85

- Before splitting one agent into multiple agents, verify that the existing skill/modality isolation within a single agent isn't already sufficient. A separate agent adds ceremony — its own frontmatter block, permission set, REGISTRY row, and delegation path — that must be justified by clear value beyond what a mode-specific skill loaded by the original agent already provides. Confidence: 0.80

- Avoid concrete real-world examples in agent/subagent prompts and skill documentation. Use generic, abstract descriptions of behavior patterns instead. Real examples (class names, entity names, symbol names from the actual codebase) create anchors that skew the agent's behavior — it overfits to the example rather than generalizing to the caller's actual input. Confidence: 0.90
- When verifying skill names, directory names, or file existence within the project, always check the actual filesystem (glob, ls) rather than relying on cached or stale knowledge from prior sessions. The filesystem is the source of truth; names may have been renamed since the last observation. Confidence: 0.85
- Skill files scoped to a specific agent family should be prefixed with that family's name (e.g., `coder-` for skills used by `p-coder-*` agents). This namespaces skills by their consumer and makes the ownership boundary visible at a glance. Confidence: 0.90
- In agent prompts, subagent invocation instructions should use compact inline format (e.g., `- s-doc-explorer → Task: <description> | Concepts: <list> | Domains: all`) rather than verbose fenced `Tool: task` code blocks. The inline format is scannable, saves ~75% of the vertical space, and still conveys the subagent name, prompt variables, and when to use it. Confidence: 0.88
- When renaming a skill, subagent, or any named entity that other files reference, fix every stale reference across the entire project in the same change batch — agent files, REGISTRY.md, FINDINGS.md, and any other agent rows that list it. Don't leave the registry or other agents pointing to old names. Confidence: 0.85

- Prefers delegation to a dedicated subagent over implementing functionality directly in the parent/primary agent. When a choice exists between "do it yourself" and "delegate," the user defaults to delegation — even if it means adding a new operation to the subagent. Confidence: 0.85

- For analytical/review tasks where the main agent is capable of doing the work itself given the right process and instructions, prefers packaging those instructions as a loadable skill over delegating to a subagent. Subagents can hallucinate work they never performed (claiming edits and report files that don't exist), creating a trust/verification gap. A skill loaded directly into the main agent eliminates this problem — the work happens inline, transparently, and is immediately verifiable. Confidence: 0.88

- When a specialized skill is loaded, it should act as a context reset — the skill's domain becomes the agent's sole focus, and unrelated context is dropped. The skill is a mode switch, not an augmentation. The agent should "forget everything else" and operate exclusively within the skill's scope until the task is complete. Confidence: 0.82

- Reports should itemize specific findings or changes, not just summarize with aggregate counts. The user needs to know exactly what was found or changed (which specific mismatches, what was updated in each file), not just that N things were found. Aggregate summaries without itemized detail are not actionable. Confidence: 0.80

- Delegation to a subagent is NOT warranted when the task is straightforward mechanical validation the main agent can do with tools it already has (e.g., read a YAML file, verify presence/parseability/structure — via `get_files`). Adding a new subagent operation for such checks adds unnecessary indirection. Reserve delegation for tasks that require specialized tools, domain expertise, or multi-step reasoning the main agent lacks. Confidence: 0.82

- In multi-mode agents, keep mode boundaries clean: one mode should not consume artifacts produced by a different mode unless there is an explicit, documented handoff contract between them. The default expectation is that each mode is self-contained. Cross-mode artifact loading (e.g., Generate Mode reading a stale DevOps report) adds conditional complexity that needs strong justification — "defense-in-depth against an improbable case" is not enough. Confidence: 0.85
- In shared-core / mode-protocol architectures, when the shared core references a skill as a dependency, every mode-specific protocol or procedure that needs that skill must contain its own explicit "load now" instruction at the appropriate execution point. A passive reference in the shared core is not sufficient — the concrete load directive must live in the execution path, not just be implied. Confidence: 0.85
- Apply the same architectural patterns to analogous agent families. When one family (e.g., coder) uses a shared-core + separate mode-agents split, other families with the same multi-mode structure should follow suit. The user expects architectural consistency across the system — don't defend one pattern for one family and the opposite for another. Confidence: 0.85
- When the user points out that your reasoning contradicts a pattern already established elsewhere in the system, acknowledge the inconsistency directly and adopt the consistent approach. Don't defend the original position — the user values reasoning that generalizes. Confidence: 0.82
- When an agent or subagent has access to MCP tools (get_files, grep_files, etc.), the native `read` permission should be scoped to external files only — files outside the project root that MCP cannot reach (e.g., `/tmp/`). MCP tools are project-bound; native `read` fills the gap. If both are granted, the agent prompt should document the boundary explicitly so the agent knows which tool to use for which path. Confidence: 0.85
- When creating new agent or skill capabilities for the Pheidipp project, prefers Command Code-compatible format placed under `.commandcode/` (`.commandcode/agents/` for agents, `.commandcode/skills/<name>/SKILL.md` for skills). The existing `.opencode/` directory continues to house the OpenCode-native agents; new work goes in `.commandcode/`. Confidence: 0.85
- The prompt/prompt-architect role is the sole authority on the agent and skill ecosystem — one dedicated entity owns managing all agents, subagents, and skills, maintaining the registry and findings, and making architectural decisions about the prompt layer. Confidence: 0.82
- When an agent owns maintenance artifacts (registries, findings, indices), updates to those artifacts should be automatic side effects of the agent's operations, not separate manual steps. The agent should maintain its own artifacts without being explicitly told — if it creates, modifies, or deprecates an agent or skill, the registry update is part of the same operation. Confidence: 0.85
- When auditing agent prompts, don't flag missing references to files that the runtime automatically injects into every agent's context (e.g., OpenCode's AGENTS.md or instructions files). Checking whether agents explicitly reference auto-loaded files is pointless — the runtime already provides them. Know what the platform injects before auditing for gaps. Confidence: 0.85
- Support/meta files belonging to a Command Code agent (registries, findings, tool catalogs) should live under `.commandcode/meta/` as the canonical location. When migrating from `.opencode/meta/`, copy rather than move — keep `.opencode/meta/` as a synced legacy copy for consumers that still reference the old paths. Deleting the originals breaks backward compatibility. Confidence: 0.88

- When migrating files between domains/locations, prefer `cp` over `mv` — keep the old location as a synced copy until all consumers are updated. Confidence: 0.85

- When a canonical domain/location is established for an agent's artifacts, the agent should maintain only that canonical location. Legacy copies in deprecated locations are not the agent's responsibility — don't sync, don't maintain, don't worry about them. The deprecation of legacy locations is a separate concern that belongs to whoever owns the migration, not to the agent managing the canonical domain. Confidence: 0.88
- Don't even mention deprecated/legacy locations in agent prompts — not even to disclaim responsibility. Referencing a legacy path, even to say "don't use this," adds noise and creates ambiguity about what the agent should care about. The prompt should only name the canonical domain it owns. Confidence: 0.85
- Agent maintenance artifacts (registries, findings, indices) should be clean current-state documents — not accumulations of historical context from predecessor agents. When an agent is replaced or deprecated and a new one takes over its artifacts, wipe them to reflect only the present state. Don't carry forward the old agent's narrative, working notes, or historical entries. The slate is clean and the new agent owns it fresh. Confidence: 0.85

- Prefers consolidating redundant meta artifacts into a single source of truth. When two maintenance files have overlapping content (e.g., REGISTRY.md and FINDINGS.md both tracking agent status), merge them rather than maintaining both. Each artifact should have a distinct, non-overlapping purpose — if one can absorb the other's content, drop the redundant file. Confidence: 0.88

- Command Code agents that are invoked as subagents need `permissionMode: bypass` in their frontmatter. Without it, the agent hits permission denials because it cannot prompt the user for approval from within a subagent context — the approval dialog has no user to present to. This is not a tool-permission issue but a subagent execution-mode issue. Confidence: 0.85
- Monitors model costs and proactively replaces expensive models with cheaper alternatives. When a model's pricing is prohibitive, the user will seek out all agents using it and swap them to a more cost-effective option rather than absorbing the cost. Confidence: 0.78

- `minimax-m3` is the preferred cost-effective fallback model. When an agent needs a cheaper model, minimax-m3 is the default replacement — the user reaches for it by name rather than asking for alternatives. Confidence: 0.72

- Prefers seeing agent reasoning/chain-of-thought streamed during execution for transparency — wants to observe the model's thinking in the prompt, not just the final output. Confidence: 0.70
- For Command Code agents that produce analysis or reports, `showOutput: true` should be set in frontmatter so the full agent response renders inline rather than just a terse "Done" summary. The default one-liner wastes the agent's work — the user wants to see what was actually produced, especially after long-running invocations. Confidence: 0.85
- Agents that produce analysis, review findings, or any substantial output should persist it to a report file on disk — never rely solely on inline/streaming responses. The inline response should be a terse one-liner with the report path and key counts; the report file is the real deliverable. This guarantees findings are accessible even when the parent agent's response truncates or summarizes the subagent's output, and the user can open the report file independently. Confidence: 0.85
- Agents performing reviews, audits, cross-checks, or any analysis of existing artifacts should default to read-only mode — report findings, do NOT apply fixes. The user must explicitly request changes before any files are modified. "Automatic maintenance" rules that trigger edits without explicit approval are dangerous and produce scope creep (e.g., a registry cross-check agent editing subagent prompt files). The only exception: when designing a NEW agent or skill, registry updates are part of the creation workflow. Confidence: 0.92
- When unauthorized or unexpected changes have already been made (e.g., an agent went off-script and modified files), do NOT preemptively revert them. First show the diff or a categorized change summary so the user can evaluate what happened and decide what to keep or discard. Don't assume all unintended changes should be rolled back — some may be valid fixes the user wants to preserve. The user wants to inspect before reverting, not undo blindly. Confidence: 0.85
- Never trust a subagent's self-reported summary of what work it did — always verify with git and filesystem evidence before relaying results to the user. Agents can hallucinate convincing, detailed summaries of modifications they never actually made (e.g., claiming to have written a report file or edited REGISTRY.md when no files were touched). Before reporting "the agent did X," confirm with `git diff`, file existence checks, and modification timestamps. The user expects evidence-backed claims, not agent self-reports taken at face value. Confidence: 0.92
- Never revert, discard, or `git checkout` the user's uncommitted working-tree changes without explicit confirmation. Uncommitted changes represent the user's work in progress — potentially hours of effort — and are not yours to touch. Treat dirty working-tree files as sacrosanct unless the user explicitly authorizes their removal. This is distinct from reverting unauthorized changes an agent itself made; the user's own uncommitted work has a completely different status and must be preserved. Confidence: 0.95

- Subagent execution parameters (timeouts, tool call budgets, retry logic) should be derived internally by the subagent from observable task characteristics, never exposed as caller-facing input. The caller provides task parameters (what to do — selectors, plan-id, label), not execution parameters (how to do it — timeout duration). Exposing internal operational concerns as caller inputs blurs the delegation boundary and leaks implementation details that the caller shouldn't need to know about. The subagent should auto-compute its own operational settings: e.g., derive shell timeout from the number of test selectors it received. Confidence: 0.85

- When distilling test results into a summary ("juice") for downstream analysis, the summary must include error details — not just which tests failed. Failure identification alone is insufficient; the actual error messages/content are what make the summary actionable for a test-analyzer agent. A juice that omits error content starves the downstream analysis of the information it needs. This is a hard, non-negotiable requirement — the user explicitly rejected approaches that cannot deliver failure reasons in the juice. Confidence: 0.90
- Prefers the lighter alternative when given a choice between a robust-but-heavier solution and a simpler one that covers most cases. Even when warned that the lighter option may not handle all edge cases (e.g., pytest `-rfE` flag vs JUnit XML for teardown-error reasons), opts for the minimal change that requires zero new infrastructure. Weighs simplicity and zero-new-dependency heavily against theoretical completeness. Confidence: 0.82
- When applying changes across multiple files, prefers file-by-file incremental application (one file per approvable unit) over large parallel edit batches. A batch of 15 edits across 6 files was denied wholesale; the user chose "file-by-file" when offered that vs. a review-first approach. Each file's edits land as a separate, reviewable unit so the user can approve or halt at any boundary. Confidence: 0.70
- When a proposed solution fails empirically, pivot to the next known alternative immediately — don't keep probing why the first approach didn't work. The user has low tolerance for iterative troubleshooting cycles ("stop going in circles") when a viable fallback is already on the table. Move to the fallback, don't investigate the failed path further. Confidence: 0.88
- Wants to empirically validate a solution's actual output before any implementation changes are applied — see the real data/output first, then commit to the approach. This goes beyond reasoning-based explanation: the user asks "how can I generate X to see the output" before edits land. Provide a way to inspect real results first, then apply. Confidence: 0.85
- Keep diagnostic and verification steps as simple as possible — a single minimal command using one test case, not a multi-step probe script. The user reacts with frustration ("jeeezzz", "dont overthink this") when given complex multi-command probes when a one-liner would suffice. Strip the verification to its essence: one command, one test, paste the output. Confidence: 0.88
- Prefers to run verification and diagnostic commands themselves rather than having the assistant execute them via shell tools. When the assistant proposed running a multi-step probe itself, the user redirected to "give me a simple command i can run and tell you the output." Provide a single copy-pasteable command the user can run in their own terminal; they'll paste the output back for analysis. This also avoids issues where the assistant's shell invocations have environment or permission problems (e.g., login-shell flags causing unintended behavior). Confidence: 0.80
- When providing shell commands (especially `docker compose exec`), mirror the exact invocation patterns from existing working scripts in the project rather than inventing new command styles. The user's environment has specific quoting and interaction requirements that existing scripts already account for. The assistant's novel patterns (e.g., `bash -lc`, direct exec without `bash -c`) repeatedly failed; the working approach was to copy the proven `docker compose exec -e ... api bash -c "..."` pattern from run-tests.sh. Confidence: 0.82
- Pytest `::` node selectors (e.g., `tests/foo.py::TestClass::test_method`) do NOT work through `docker compose exec` in this environment — they silently cause all tests to run regardless of quoting (`bash -c`, `bash -lc`, direct exec all fail). The working pattern is to pass **file paths** as selectors instead (e.g., `tests/foo.py`), which is what the project's own subagents already do. When a single method must be targeted, run the whole file and accept the extra seconds as the cost of the environment's limitation. Confidence: 0.90
- Pytest suppresses failure reason details in the short test summary when stdout is not a terminal (e.g., output redirected to a file). The `FAILED` lines show only the test node ID, not the `- AssertionError: ...` suffix. Since test-runner scripts redirect output to `/tmp/...txt` files, the reason suffix is never present in the captured output. Relying on `-q` or `-rfE` flags to surface failure reasons does not work in non-interactive contexts. Confidence: 0.88
- When providing multi-line command blocks for the user to run, format them as a single paste-able unit that can be pasted all at once — bash runs them line by line sequentially. Don't present commands as a sequence of separate steps requiring one-at-a-time execution. The user asks "can I paste the whole thing?" and expects the answer to be yes. Confidence: 0.80
- Prefers consolidating multi-step operational workflows into a single script entry point rather than leaving orchestration to the caller. When a process has sequential steps (e.g., run pytest, then parse JUnit XML), the user expects one script/command to handle the full pipeline end-to-end. Asks "can't the run_tests script do everything in one go?" rather than running steps separately. Scripts should be self-contained pipelines, not fragments the caller assembles. Confidence: 0.85
- Prefers to manually validate infrastructure/script changes in their own terminal before any dependent agent-prompt updates are applied. Sequencing: modify scripts → user runs and inspects real output → only then update agent prompts that reference those scripts. "I want to test this manually before we do updates to the agent." Don't propose agent-prompt edits until the user confirms the script works empirically. Confidence: 0.82
ecution. The user asks "can I paste the whole thing?" and expects the answer to be yes. Confidence: 0.80
